{
  "dependencies": {
    "bootstrap": "~3.3.4",
    "firebase": "^4.12.0",
    "mithril": "~0.1.34"
  },
  "devDependencies": {
    "ts-loader": "^4.1.0",
    "typescript": "^2.8.1",
    "webpack": "^4.5.0",
    "webpack-cli": "^2.0.14"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "node_modules/.bin/webpack --mode development --watch  # watch out -- scripts will need perms set before they can be run",
    "build": "node_modules/.bin/webpack --mode development && chmod a+x dist/*"
  }
}
